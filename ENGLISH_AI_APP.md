# English AI Learning App

هذا الفرع مخصص لتطبيق تعلم اللغة الإنجليزية بالذكاء الاصطناعي، وهو منفصل عن فرع `main` حتى لا يتأثر مشروع غرفة الطوارئ.

## المراحل المنجزة

### المرحلة الأولى

- التسجيل وتسجيل الدخول عبر Supabase Auth.
- إعداد ملف المتعلم والهدف واللهجة والاهتمامات والوقت اليومي.
- NestJS API مرتبط بـ PostgreSQL/Supabase.

### المرحلة الثانية

- اختبار تحديد مستوى تكيفي CEFR من A1 إلى C2.
- تقييم مستقل للقواعد والمفردات والقراءة والاستماع.
- تخزين كل جلسة وإجابة ونتيجة داخل قاعدة البيانات.

### المرحلة الثالثة

- صفحة رئيسية ذكية ومستوى مستقل لكل مهارة.
- هدف يومي وXP وسلسلة تعلم ومراجعات مستحقة.
- مسار أسبوعي شخصي من 7 إلى 14 نشاطاً.
- خوارزمية تراعي نقاط الضعف والهدف والاهتمامات.

### المرحلة الرابعة

- محرك دروس تفاعلي قابل للحفظ والاستئناف.
- الاستماع والقراءة والمفردات والأسئلة والتصحيح من الخادم.
- حفظ المحاولات والدرجات وإكمال المسار تلقائياً.

### المرحلة الخامسة

- مختبر تحدث بثلاثة أوضاع: القراءة الموجهة، Shadowing، وRole-play.
- تسجيل WAV من Flutter ورفع آمن بحد 15MB.
- تفريغ اختياري عبر OpenAI وتقييم نطق وفونيمات اختياري عبر Azure Speech.
- درجات الدقة والطلاقة والاكتمال والتنغيم والكلمات والفونيمات.
- محادثة متعددة الأدوار مع بديل محلي للتطوير عند غياب مفاتيح المزودين.
- تخزين التسجيلات في Supabase Storage خاص، وإنشاء الحاوية آلياً من الخادم.
- إنشاء الحساب وتأكيد البريد وإعادة إرسال التأكيد وتسجيل الدخول والخروج.
- استعادة كلمة المرور وتعيين كلمة جديدة عبر رابط Recovery آمن.
- أدوار `learner` و`admin` في PostgreSQL مع `AdminGuard` وسجل تدقيق إداري.
- لوحة إدارة لمؤشرات المستخدمين والدروس والنشاط ومحاولات التحدث.
- المدير يحتفظ بدور المتعلم ويمكنه استخدام كل الدروس والاختبارات.
- سكربت وGitHub Workflow لإنشاء أو ترقية المدير من Secrets دون تخزين كلمة المرور في المصدر.
- Supabase migration رقم `202606260005_speaking_and_admin.sql`.

## التحقق

- NestJS build: ناجح.
- اختبارات الخادم: 18 من 18 ناجحة.
- Production dependency audit: لا توجد ثغرات مسجلة.
- فحص YAML وبنية واستيرادات Dart: ناجح.
- فحص تسرب بيانات الدخول والمفاتيح: ناجح.
- فحص تطابق نسخ migrations وسلامة الحزم المضغوطة: ناجح.

تعذر تشغيل `flutter analyze` و`flutter test` داخل بيئة الإنشاء لعدم توفر Flutter SDK، لذلك تمت إضافة workflow مستقل لتشغيلهما على GitHub.

## الحساب الإداري

بيانات الدخول لا تُكتب في المستودع. أضف الأسرار التالية ثم شغّل workflow باسم `Bootstrap Administrator`:

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `DATABASE_URL`
- `ADMIN_EMAIL`
- `ADMIN_PASSWORD`

عند وجود الحساب مسبقاً تتم ترقيته إلى مدير، ولا تتغير كلمة مروره إلا عند تفعيل `force_password_update`. بعد الترقية يجب تسجيل الخروج ثم الدخول لتجديد الجلسة.

## المراحل الأساسية المتبقية للإصدار الأول

تبقى أربع مراحل رئيسية:

1. الكتابة الذكية والتصحيح النحوي والأسلوبي.
2. المفردات والإملاء ونظام FSRS.
3. لوحة صناعة المحتوى والإشعارات والتحليلات المتقدمة.
4. الاختبارات الشاملة والأمان والنشر الإنتاجي ومتاجر التطبيقات.

لا ترفع كلمة مرور قاعدة البيانات أو Service Role Key أو مفاتيح الذكاء الاصطناعي أو بيانات المدير داخل الملفات العامة.